"use client"; // directive

import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={() => {alert("Added Cart")}}>Add to Cart</button>
    </div>
  )
}

export default AddToCart